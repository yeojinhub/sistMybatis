<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="day0602">

<!-- insert -->

<!--
	insert 외부 값 없이 추가 : parameterType 속성을 사용하지 않음,
	고정적인 형식으로 값을 추가할 때 사용.
	SqlSession.insert("아이디");
-->
<insert id="insertCpEmp7">
INSERT INTO CP_EMP7(EMPNO, ENAME, JOB, SAL, COMM, HIREDATE)
VALUES (SEQ_BOARD.nextval, '이장훈', '개발자', 3000, 0, SYSDATE )
</insert>

<!--
	insert 단일형 추가
	java 에서 사용하는 모든 데이터형을 직접 사용가능
	기본형 데이터형이나 Wrapper 는 getter method 가 없으므로 값을 사용하는 쿼리에서는 아무말 대잔치?
-->
<insert id="insertCpEmpno" parameterType="int">
INSERT INTO CP_EMP7(EMPNO, ENAME)
VALUES ( #{ EMPNO }, '이여진' )
</insert>

<!--
	insert 복합형 추가
	#{ getter 명 }
-->
<insert id="insertCpDTO" parameterType="day0602.CpEmpDTO">
INSERT INTO CP_EMP7(EMPNO, ENAME, JOB, SAL, COMM, HIREDATE)
VALUES (SEQ_BOARD.nextval, #{ ename } , #{ job } , #{ sal }, #{ comm }, SYSDATE )
</insert>

</mapper>